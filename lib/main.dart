import 'package:flutter/material.dart';

void main() {
  runApp(const MaterialApp(
    title: "Flutter Class",
    home: Center(
        child:
            Text('Hello, world!', textDirection: TextDirection.ltr)), // 文本居中显示
  ));
}
